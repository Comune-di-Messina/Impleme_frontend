{% if items %}
<ul {{ attributes.addClass(['list-inline', 'mb-0', 'd-flex', 'flex-column', 'flex-md-row']) }}>
  {% for item in items %}
    <li class="list-inline-item">
      <a href="{{ item.url }}" title="{{ item.title }}">{{ item.title }}</a>
    </li>
  {% endfor %}
</ul>
{% endif %}